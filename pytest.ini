[pytest]
# Prevent ROS Humble's launch_testing pytest plugin from being loaded
# (it requires lark which is not installed in this venv)
addopts = --import-mode=importlib -p no:launch_testing -p no:launch_testing_ros_pytest_entrypoint -p no:anyio
